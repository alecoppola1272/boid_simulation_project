#include <cmath>
#include <iostream>
#include <vector>

struct dati {
  std::vector<double> velox{0.,0,};
  std::vector<double> position{0.,0.};
  double const ds;
}

class Boids {
 public:
  Boids() : {}
  std::vector<dati> velox {}
}

if (d < abs(x_bi - x_bj)) {
}